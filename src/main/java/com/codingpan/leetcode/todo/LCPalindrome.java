package com.codingpan.leetcode.todo;

public class LCPalindrome {

    // Top 100 Algm
    // add min count of chars to form a palindrome
    // algm: dp[N][N], dp[i][j]代表子串 i，j中最少添加几个字符可以是其str[i..j]整体是回文
    //
    public String generatePalindrome(String s) {
        return s;
    }

    // find the longest palindrome subsequence in a string
    // algm1: s1 is self, s2 is reverse s1,
    // longest common subsequence between s1 and s2 is the longest palindrome subsequence in s1
    public String findLongestPalindrome(String s1) {
        return s1;
    }

    public static void main(String[] args) {
        LCPalindrome solu = new LCPalindrome();
    }
}
